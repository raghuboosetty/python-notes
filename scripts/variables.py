# variable names should have underscores
# dynamically typed, meaning that you can use a variable without declaring it first
# cannot interpolate/concatenate string with int/bool. Use typecast to achieve that
character_name = "Tom"
character_age = 50
is_male = True
print(character_name + " " + str(character_age) + " " + str(is_male))

from math import *
print(sqrt(36))
